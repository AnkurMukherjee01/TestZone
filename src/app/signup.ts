export class SignUp {
    constructor(
        public email: string,
        public password: string,
        public firstName: String,
        public lastName: String,
        public confirmPassword: String
      ) {  }
}